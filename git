git init                                        -> iniciar um repositorio git vazio // cria uma pasta .git 
git add .                                       -> adicionar tudo que estiver na pasta 
git config --global user.email "you@example.com"
git config --global user.name  "Your Name"
git commit -m "Primeiro arquivo do repositório" -> leva as mudanças de um ambiente local para o repositório no git, permitindo ainda a inserção de uma mensagem descritiva
git status                                      -> exibe as condições do diretório de trabalho e da área de staging
git log -n 10                                   -> exibe o histórico das últimas "n" alterações
git remote add origin                           -> linka ao repositório online

git push -u origin main
git push origin main

git remote remove origin 

new test with commentary 

git branch -m 

Não aconselhável fazer:
git commit --amend -m "Novo comentário"
git push origin main --force    



Cria uma nova branch:
git checkout -b nomeDaNovaBranch

Trocar de branch
git checkout nomeDaBranch

git add . -> para todos os arquivos
git add nomeDoArquivo -> somente para os arquivos selecionados


merge =-> aprovar um pull request de uma branch terceira para a main

